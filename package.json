{
  "name": "clicsalud-auto-login",
  "version": "1.0.0",
  "description": "Standalone Cloudflare Worker for automated ClicSalud+ authentication",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
